apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  namespace: development
  labels:
    app: my-app 
spec:
  selector:
    app: my-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: NodePort
